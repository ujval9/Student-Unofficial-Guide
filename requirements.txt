# sentence-transformers/transformers bumped to 5.x so they accept
# huggingface-hub>=1.2, which gradio>=6.9 requires (the 3.4.1/4.57 combo pinned
# hub<1.0 and conflicted with gradio).
sentence-transformers>=5.6.0
transformers>=5.0
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface — add whichever you use for Milestone 5:
gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4

# Document ingestion — HTML parsing/cleaning for .html sources:
beautifulsoup4==4.12.3
